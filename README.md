# Ubic-Service-Node.js

Provide a simple service to run node.js apps as Ubic service.

## Synopsis 

Since Node.js version 0.6 the cluster API at its core.
This service based on simplest way to run your own app 

  `node /path/to/app.js`


